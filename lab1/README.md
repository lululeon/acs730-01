# Lab 1

Instructions for this section will be provided in class and on Blackboard when we reach it.

Put your work for Lab 1 in this folder.

Update to readme (did not see full instructions for this step, thought we just edit the file)
The scripts:
- lab1/scripts/create-security-group.sh : this creates security group allowing ssh (port 22)
- lab1/scripts/create-instance.sh : creates the actual ec2 instance setting the type, image, instance profile.
- lab1/scripts/delete-instance.sh : will be used to delete the instance afterward
- lab1/scripts/delete-security-group.sh : will be used to delete the security group afterward

